import localFont from "next/font/local";

/** Rubik Bold — Hebrew subset (Google Fonts, OFL) */
export const logoHebrew = localFont({
  src: "../../fonts/rubik-bold-hebrew.woff2",
  variable: "--font-logo-hebrew",
  display: "swap",
  weight: "700",
});
